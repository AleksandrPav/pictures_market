import "./products.scss";

const Products = (parentCategories, categories) => {
  console.log(parentCategories);
  console.log(categories);
  return (
    <div className="products">
      <h1>Products</h1>
    </div>
  );
};

export default Products;
